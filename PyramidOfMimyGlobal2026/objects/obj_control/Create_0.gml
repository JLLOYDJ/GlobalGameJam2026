#region //viewport stuff
//default window size
//window_set_fullscreen(true);
window_center();


//setting default camera
view_camera[0] = view_camera[0];
#endregion

start = true;

alpha = 0;
fade = true;